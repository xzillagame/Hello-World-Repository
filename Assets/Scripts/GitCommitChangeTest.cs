using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GitCommitChangeTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("This is a test script to check commit changes");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
